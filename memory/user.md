# User — Martin

- Builds **Amgi**, an offline-first Anki-compatible iOS/iPadOS/macOS client
  (plus EPUB reader). Solo developer; repo `~/Documents/GitHub/amgi`.
- Comfortable with Swift 6 strict concurrency, SPM multi-package layouts, and
  Rust FFI; delegates ML/python dev-time tooling to the agent but runs heavy
  jobs locally (network-restricted sandboxes are a known constraint).
- Prefers specs written up front in markdown (`deck-icon-picker-spec.md`),
  with amendments delivered as short instructions on top.
- Wants to be **flagged** when spec assumptions break against measured
  reality (e.g. thresholds that never fire, bundle-size surprises) rather
  than silently followed.
